name: Apply safe data guards

on:
  push:
    branches:
      - codex/safe-data-guards
    paths:
      - .github/workflows/apply-safe-data-guards.yml
      - tools/apply-safe-data-guards.mjs

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply safe data guard changes
        run: node tools/apply-safe-data-guards.mjs index.html

      - name: Remove temporary transformer
        run: |
          rm -f tools/apply-safe-data-guards.mjs
          rm -f .github/workflows/apply-safe-data-guards.yml

      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git status --short
          git add index.html tools/apply-safe-data-guards.mjs .github/workflows/apply-safe-data-guards.yml
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "Add safe data guards"
            git push
          fi
